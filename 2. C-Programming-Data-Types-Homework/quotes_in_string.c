#include <stdio.h>

int main(void)
{
    char *Input = "The \"use\" of quotations causes difficulties. \\n, \\t and \\ are also "
                  "special characters.";

    printf("%s", Input);
    
    return (0);
}
